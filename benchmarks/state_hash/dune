(executable
 (name state_hash)
 (libraries
  core
  core_bench
  core_unix.command_unix
  rely.lib
  tezos_rpc
  node
  protocol
  tezos_interop)
 (preprocess
  (pps ppx_deriving_yojson ppx_let_binding)))

(rule
 (alias bench_state_hash)
 (action
  (run %{exe:state_hash.exe})))
