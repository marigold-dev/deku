(data_only_dirs flextesa_chain node_modules nix)

(env
 (static
  (flags
   (:standard -ccopt -static))
  (ocamlopt_flags
   (:standard -O3 -unbox-closures)))
 (release
  (ocamlopt_flags
   (:standard -O3 -unbox-closures))))
