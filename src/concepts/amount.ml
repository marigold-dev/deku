open Deku_stdlib
open N

type amount = N.t
type t = amount

let show n = show n
let pp fmt n = pp fmt n
let equal a b = equal a b
let compare a b = compare a b
let zero = zero
let one = one
let of_n x = x
let to_n x = x
let ( + ) a b = a + b
let ( - ) a b = a - b
